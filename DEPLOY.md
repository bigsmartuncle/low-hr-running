# 户外使用部署说明

本目录是完整的静态 PWA，可发布到任何支持 HTTPS 的静态托管平台。

推荐发布到 GitHub Pages、Cloudflare Pages、Netlify 或 Vercel。发布后：

1. 用 iPhone Safari 打开 HTTPS 地址。
2. 在“设置”中导入原地址导出的训练数据。
3. 点击 Safari 分享按钮，选择“添加到主屏幕”。
4. 首次联网打开一次后，应用文件会缓存到手机，可离线查看和记录。

注意：训练数据保存在当前网址对应的 Safari 本地存储中。更换网址或设备前应先导出备份。
